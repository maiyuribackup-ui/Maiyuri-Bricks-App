/**
 * Re-export of the OneHub palette for `src/` components.
 * Source of truth stays in app/onehub/theme.ts (the `@` alias only covers
 * src/, so feature components import the tokens through this shim).
 */
export { onehub } from "../../app/onehub/theme";
