{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps --registry=https://registry.npmjs.org --no-audit",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/odoo/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
