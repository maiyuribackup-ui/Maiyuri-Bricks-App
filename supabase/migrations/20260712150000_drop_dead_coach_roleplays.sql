-- coach_roleplays: created for a roleplay feature that was never built.
-- Zero rows, zero code references (verified 2026-07-12). rate_master was
-- also audited but KEPT: it holds 11 live rows.
drop table if exists coach_roleplays;
